version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - PORT=8000
    env_file:
      - .env
    volumes:
      # Mount data directory for SQLite database
      - ./data:/app/data
    restart: unless-stopped
